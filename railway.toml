# Railway：明确用 Node 跑 Express API（避免被误判为纯静态站）
# 文档：docs/DEPLOY_RAILWAY.md

[build]
builder = "NIXPACKS"

# 直接跑 tsx，避免用 npm start 做父进程；平台发 SIGTERM 时 npm 常会误报 command failed
[deploy]
startCommand = "tsx server/index.ts"
